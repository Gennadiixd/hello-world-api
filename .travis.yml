language: node_js

node_js:
  - 12

services:
  - docker

before_install:
  - docker build -t hello-world-api .

deploy:
  provider: script
  script: bash docker push gennadiixd/hello-world-ci-cd
  on:
    branch: master
